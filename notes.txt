
// WARN: we don't have RewardEscrowV2, it's new contract
escrowedReward = RewardEscrowV2.balanceOf(walletAddress)

tokenSaleEscrow = 0

totalEscrowBalance = escrowedReward + tokenSaleEscrow

issuableSynths = Synthetix.maxIssuableSynths(walletAddress)

debtEscrowBalance = debtBalance + (totalEscrowBalance / hznRate / targetCRatio) - issuableSynths

addedTransferable = (amountToBurn - debtEscrowBalance) / hznRate / targetCRatio

TransferableAmountFromBurn = transferable + addedTransferable